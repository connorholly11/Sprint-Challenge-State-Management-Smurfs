

export const initialState = {
    smurfs: [],
    isFetching: false,
    error: ''
}


export const SmurfReducer = (state = initialState, action) => {   
    switch(action.type){

        default:
            return state;
    }
}